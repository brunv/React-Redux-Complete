import React from 'react';
import Modal from '../Modal'

const StreamDelete = () => {
    return (
        <div>
            Stream StreamDelete
            <Modal />
        </div>
    );
};

export default StreamDelete;